{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "desktop-dialog",
  "description": "Desktop-only file picker access for attachments and project selection.",
  "windows": ["main"],
  "platforms": ["macOS", "windows", "linux"],
  "permissions": ["dialog:default", "dialog:allow-open"]
}
